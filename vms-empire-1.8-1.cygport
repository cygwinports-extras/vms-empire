DESCRIPTION="Solitaire Empire game"
HOMEPAGE="http://www.catb.org/~esr/${PN}/"
SRC_URI="http://www.catb.org/~esr/${PN}/${P}.tar.gz"

src_compile() {
	lndirs
	cd ${B}
	cygmake CFLAGS="${CFLAGS} -I/usr/include/ncurses"
}

src_install() {
	cd ${B}
	dobin ${PN}.exe
	newman empire.6 ${PN}.6
}
